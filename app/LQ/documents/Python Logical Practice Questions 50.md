Got it! You’d like a set of **50 Python logic practice questions**—similar to number palindrome, pyramid printing, factorial, etc. Here’s a curated list that covers loops, conditionals, recursion, string manipulation, and basic algorithms:

---

## 🔢 Python Logical Practice Questions (50)

1. Check if a number is a palindrome.  
2. Print a pyramid pattern of stars.  
3. Find factorial of a number (iterative & recursive).  
4. Generate Fibonacci series up to `n` terms.  
5. Reverse a string without using slicing.  
6. Count vowels and consonants in a string.  
7. Check if a number is prime.  
8. Find the sum of digits of a number.  
9. Print multiplication table of a given number.  
10. Find the largest of three numbers.  
11. Check if a string is an anagram of another.  
12. Find GCD and LCM of two numbers.  
13. Print Pascal’s triangle.  
14. Find Armstrong numbers in a given range.  
15. Convert decimal to binary without built-in functions.  
16. Find the second largest number in a list.  
17. Sort a list without using `sort()` or `sorted()`.  
18. Remove duplicates from a list.  
19. Find the frequency of each character in a string.  
20. Check if a string is a pangram.  
21. Print prime numbers between 1 and 100.  
22. Find the sum of all even numbers in a list.  
23. Find the sum of all odd numbers in a list.  
24. Print a diamond pattern using stars.  
25. Check if a number is perfect (sum of divisors = number).  
26. Find the digital root of a number.  
27. Count words in a sentence.  
28. Find the longest word in a sentence.  
29. Check if a string is palindrome.  
30. Print all substrings of a string.  
31. Find the missing number in a sequence.  
32. Find the duplicate number in a list.  
33. Rotate a list by `k` positions.  
34. Find the intersection of two lists.  
35. Find the union of two lists.  
36. Check if two strings are rotations of each other.  
37. Print the first non-repeating character in a string.  
38. Find the maximum occurring character in a string.  
39. Check if a number is a strong number (sum of factorial of digits = number).  
40. Print the sum of series `1 + 1/2 + 1/3 + ... + 1/n`.  
41. Generate a random number without using `random` module.  
42. Swap two numbers without using a temporary variable.  
43. Find the median of a list.  
44. Find the mode of a list.  
45. Print the ASCII value of a character.  
46. Convert a string to title case without using `title()`.  
47. Find the largest palindrome substring in a string.  
48. Print the prime factors of a number.  
49. Find the sum of squares of first `n` natural numbers.  
50. Print a hollow square pattern using stars.

---

✨ These cover a wide range of logical exercises, perfect for sharpening Python skills.  
Would you like me to **provide solutions for all 50**, or would you prefer me to start with a smaller batch (say 10) and build up gradually?